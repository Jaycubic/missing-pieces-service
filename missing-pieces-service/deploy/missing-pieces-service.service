[Unit]
Description=The Missing Pieces — EQ puzzle game backend
After=network.target postgresql.service

[Service]
Type=simple
WorkingDirectory=/opt/missing-pieces-service
EnvironmentFile=/opt/missing-pieces-service/.env
ExecStart=/opt/missing-pieces-service/target/release/missing-pieces-service
Restart=on-failure
RestartSec=3
User=www-data

[Install]
WantedBy=multi-user.target
